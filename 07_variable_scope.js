var firstName;//variable decelaration
firstName="Virat";//variable initillization

var lastNmae="Kohli";//variable declaration and initilization
firstName="King Kohli";//variable update

var lastNmae; //variable re-declaration
console.log(lastNmae);

let city;//variable declaration
city="Delhi";

//let city="Mumbai";//variable decelration onitilization//Not allowed

const PI=3.14; //fix cannotChange 
//PI=3.56; //update or modify not allowed
console.log(PI);
//const PI=3.14;//redecelartion of const variable is not allowed

var num=10;
if(num==10)
{
    let word="Revision is the mother of Success";
}
//console.log(word);

function sayHi(arg)
{
    var greet="Hi Good MOrning";
    if(arg%2==0)
    {
        var isEven=true;
    }
    else{
        var isEven=false;
    }
    return isEven;
}
 console.log(isEven);
 let result=sayHi(10);
  console.log(result);

  function fun(num)
  {
    let factorial=1;
    {
        factorial=factorial*indexedDB

    }
  }
  fun(5);
  console.log(`factroal 5 is${factorial}`);
